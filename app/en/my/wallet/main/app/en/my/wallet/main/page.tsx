export const metadata = {
  title: "MYWEBBIN Spot",
  description: "Crypto Spot Platform"
}

export default function RootLayout({ children }) {
  return (
    <html>
      <body>{children}</body>
    </html>
  )
}
